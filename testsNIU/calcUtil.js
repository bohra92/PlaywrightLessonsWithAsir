module.exports = {
   add : (a,b)=>{
    console.log(`addition is ${a+b}`);
   },

   sub : (a,b)=>{
    console.log(`sunstraction is ${a-b}`);
   },

   mult : (a,b)=>{
    console.log(`multiplication is ${a*b}`);
   }

}